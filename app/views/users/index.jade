extends ../layout

block content
  table.table.table-hover.table-responsive
    caption Alle Nutzer
    thead
      tr: th Name
    tbody
      each user in users
        tr: th: p: a.link--green(href="/users/#{user.username}")
          = user.username